apiVersion: apps/v1
kind: Deployment
metadata:
  name: designsystem-app-prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: designsystem-app-prod
  template:
    metadata:
      labels:
        app: designsystem-app-prod
    spec:
      imagePullSecrets:
        - name: cerit-io-harbor-accesstoken
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: designsystem-app-prod
          image: cerit.io/design-system/designsystem-app:${IMAGE_VERSION}
          imagePullPolicy: IfNotPresent
          securityContext:
            runAsUser: 1000
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          ports:
            - name: probe-port
              containerPort: 80
          resources:
            limits:
              memory: "6Gi"
              cpu: "2"
            requests:
              memory: "3Gi"
              cpu: "2"

          # startupProbe:
          #   httpGet:
          #     path: /
          #     port: probe-port
          # readinessProbe:
          #   httpGet:
          #     path: /
          #     port: probe-port
          # livenessProbe:
          #   httpGet:
          #     path: /
          #     port: probe-port
